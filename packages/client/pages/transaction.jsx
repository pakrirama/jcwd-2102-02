import React from 'react';

import { Layout } from '../src/layout';
import { TransactionContainer } from '../src/Transcation/TransactionContainer';

const transaction = () => {
  return (
    <Layout>
      <TransactionContainer />
    </Layout>
  );
};

export default transaction;
