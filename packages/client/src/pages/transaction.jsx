import React from 'react';

import { Layout } from '../layout';
import { TransactionContainer } from '../Component/Transcation/TransactionContainer';

const transaction = () => {
  return (
    <Layout>
      <TransactionContainer />
    </Layout>
  );
};

export default transaction;
